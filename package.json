{
  "name": "yada",
  "version": "1.0.0",
  "description": "yada rocket source code",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },

  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "rocket start",
    "build": "rocket build",
    "deploy": "npm run build && firebase deploy"
  },
  "devDependencies": {
    "@d4kmor/cli": "^0.11.0",
    "@d4kmor/launch": "^0.10.0"
  }
}
